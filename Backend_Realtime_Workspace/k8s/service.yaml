# ============================================================================
# TeamSpot — Kubernetes Service
# ClusterIP service for internal communication
# ============================================================================
apiVersion: v1
kind: Service
metadata:
  name: teamspot-api
  namespace: teamspot
  labels:
    app: teamspot-api
spec:
  type: ClusterIP
  selector:
    app: teamspot-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP

---
# ============================================================================
# TeamSpot — Kubernetes Ingress
# Nginx Ingress with TLS
# ============================================================================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teamspot-api-ingress
  namespace: teamspot
  labels:
    app: teamspot-api
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    # WebSocket support for ride tracking
    nginx.ingress.kubernetes.io/websocket-services: "teamspot-api"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "86400"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "86400"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - api.teamspot.app
      secretName: teamspot-api-tls
  rules:
    - host: api.teamspot.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: teamspot-api
                port:
                  number: 80

---
# ============================================================================
# TeamSpot — Horizontal Pod Autoscaler
# Auto-scale based on CPU/Memory
# ============================================================================
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: teamspot-api-hpa
  namespace: teamspot
  labels:
    app: teamspot-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: teamspot-api
  minReplicas: 3
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 5
          periodSeconds: 15
      selectPolicy: Max

---
# ============================================================================
# TeamSpot — Pod Disruption Budget
# Ensure minimum availability during disruptions
# ============================================================================
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: teamspot-api-pdb
  namespace: teamspot
  labels:
    app: teamspot-api
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: teamspot-api