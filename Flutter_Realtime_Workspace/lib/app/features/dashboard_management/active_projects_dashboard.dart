import 'package:flutter/material.dart';

class ActiveProjectsDashboard extends StatelessWidget {
  const ActiveProjectsDashboard({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Active Projects Dashboard — coming soon')),
    );
  }
}
