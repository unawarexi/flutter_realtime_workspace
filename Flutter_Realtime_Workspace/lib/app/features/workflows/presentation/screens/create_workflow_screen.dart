import 'package:flutter/material.dart';

class CreateWorkflowScreen extends StatelessWidget {
  const CreateWorkflowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Workflow')),
      body: const Center(child: Text('Create Workflow')),
    );
  }
}
