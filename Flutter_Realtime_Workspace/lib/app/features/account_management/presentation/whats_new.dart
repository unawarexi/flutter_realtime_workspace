import "package:flutter/material.dart";
class WhatsNewScreen extends StatelessWidget {
  const WhatsNewScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("What is New")), body: const Center(child: Text("What is New — coming soon")));
}
