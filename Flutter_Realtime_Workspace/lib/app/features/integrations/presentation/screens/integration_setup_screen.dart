import 'package:flutter/material.dart';

class IntegrationSetupScreen extends StatelessWidget {
  const IntegrationSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Setup Integration')),
      body: const Center(child: Text('Setup Integration')),
    );
  }
}
