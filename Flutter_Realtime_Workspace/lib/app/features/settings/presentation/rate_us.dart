import "package:flutter/material.dart";
class RateUsScreen extends StatelessWidget {
  const RateUsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("Rate Us")), body: const Center(child: Text("Rate Us — coming soon")));
}
